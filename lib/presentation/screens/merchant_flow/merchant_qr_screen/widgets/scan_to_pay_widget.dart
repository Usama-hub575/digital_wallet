import 'package:digital_wallet/export.dart';

class ScanToPayWidget extends StatelessWidget {
  const ScanToPayWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "Scan to pay widget",
      ),
    );
  }
}
